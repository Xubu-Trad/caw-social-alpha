# Validation boundary

Alpha.19 passed **354/354 offline tests across 24 files**, with no failed, cancelled, skipped, todo or pending tests. The [97-input manifest](../evidence/CODE_SHA256SUMS.txt), [current receipt](../evidence/TEST_RESULTS.json) and [alpha.18 receipt](../evidence/ALPHA18_TEST_RESULTS.json) keep releases distinct. The bounded build rehashed 22 assets. Frontend interaction behavior is unchanged; no new browser result is claimed.

Four original Solidity 0.8.10 sources compiled with no diagnostic errors. Local execution used pinned Anvil 1.8.1, chain 31337 and London rules. The historical CAW fork completed 46 recorded steps, including four fixture configuration steps: 42 observed account states, 51 local transactions, 1,168 local RPC requests and 42 forwarded historical reads. The separate synthetic run covered 20 scenarios in 72 steps: 30 observed states, 20 verified snapshot resets, 84 local transactions and 1,527 local RPC requests, including two receipt retries; no upstream requests. Both nodes/listeners were confirmed stopped.

The independent offline oracle checks exact constructor inputs and runtime immutable patches, all requests/transactions, views, events, accounting, owners, epochs, approvals, callbacks, resets and failures. Fixed test IDs stayed isolated. NFT transfers moved control of each account's credit; transfer-return and self-transfer invalidated stale direct epochs. Direct operator spending rejected, while an operator could transfer the NFT to itself and then withdraw. Owner-wide approvals are not globally revoked by an epoch change.

Both deposit sides are now checked: the synthetic sender-fee case rejected. False returns, wrong deltas and bubbled reentrancy rolled back. Caught callbacks allowed one intended operation. Token callbacks that changed ownership or performed a transfer-return during deposit/withdrawal reverted with account/registry/token effects rolled back. Safe NFT receipt rejection and legitimate onward transfer were observed. Backing loss still blocks credited operations until the measured uncredited replenishment; dishonest balance reporting and guaranteed recovery remain open limits.

The registry is a fixed test NFT fixture, not conforming username registration or an authentic production identity source. Address/codehash pinning does not prove honest code, initial state or immutable external dependencies. Runtime registry replacement, four-argument safe transfer and custody within NFT receiver callbacks were not exercised. No signature/delegation API, DM-key transition, paid public action, historical full-block replay, consensus, root authentication, finality, freshness or permanent custody guarantee is established. Same-team review is not an external audit or community acceptance.

Historical CAW code/storage was retained; public actors were impersonated and local gas balances fabricated. Synthetic substitution was confined to a separate empty chain. Anvil's Windows Job Object caps committed memory at 512 MiB and kills the owned process on close; sampled memory/deadline/output/startup safeguards do not cap total machine memory. No real wallet, public broadcast/deployment, storage offload or deletion occurred. All 59 requirements and 15 conflicts keep their source text/status. C-001 still blocks registration; no alternative burn destination was substituted. See [account authority](ACCOUNT_AUTHORITY.md).
