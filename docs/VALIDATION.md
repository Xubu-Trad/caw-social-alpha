# Validation boundary

Alpha.10 passed **213/213 tests across fifteen files** on Node 24.20.0. The [40-file source manifest](../evidence/CODE_SHA256SUMS.txt) identifies the reviewed code. Model/economics retain alpha.3 behavior, and action signatures retain alpha.4 bytes. Current tests, syntax, build and browser checks have distinct coverage.

[Retained checkpoint comparison](CHECKPOINT_CONTINUITY.md) verifies every candidate under the anchor's captured trust before classifying exact ordered history as same, extension, rollback or conflict. Only an explicit extension changes the anchor. The 22 new tests cover divergent signed histories and recorded times, changed roots, forged signatures, captured trust, getters, immutable snapshots, spending, cancellation continuity, racing advances, invalidation and closure during verification. Separate readers can accept competing valid histories; no global freshness oracle is claimed.

Syntax passed. The supervised static build wrote and rehashed 21 files. [Current results](../evidence/TEST_RESULTS.json) record browser checks for anchor creation, explicit advancement, rollback rejection, retained cancellation, reload and explicit reconstruction, original trust after form edits, ordinary/delegated compatibility, unchanged Commons and desktop/390px/320px controls. Browser conflict/race injection and console diagnostics were not run. Multi-agent review is not independent external security review; recovery uses the same model as the writer.

All 59 requirements and 15 conflicts remain unchanged. These are supplied test keys, not proof of NFT ownership. A selected anchor may already be stale; clearing or replacing it gives up its continuity protection. No anchor is saved automatically and no signing permission is restored. Production owner lookup, wallet/contract-wallet support, durable grant/spending/replay continuity, trusted shared freshness, custody, finality, permanent retention and private messaging remain unfinished. No hosting, wallet use, real media upload or chain deployment occurred. Earlier results are historical.
