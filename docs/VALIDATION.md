# Validation boundary

Alpha.17 passed **333/333 tests across 22 files**, with no failed, cancelled, skipped or pending tests. The [74-file source/artifact manifest](../evidence/CODE_SHA256SUMS.txt) identifies the reviewed inputs. The supervised static build rehashed 22 files. [Current results](../evidence/TEST_RESULTS.json) and the [prior alpha.16 receipt](../evidence/ALPHA16_TEST_RESULTS.json) distinguish the releases.

The original CAW token source and custody probe compiled locally using the official hash-verified Solidity0.8.10 compiler under a 60-second/main-process512MiB monitored threshold. Both produced no error diagnostics. Token output equals the service's recompilation and matches captured executable bytes after substituting the observed decimals immutable; the metadata hash differs. The [source report](CAW_TOKEN_SOURCE.md) specifies this narrower result. Original historical metadata, constructor execution and a production compiler choice remain unverified.

dRPC completed25 read-only outer requests with80 calls inside one temporary simulated block. No token/block/nonce/native-balance overrides or signatures were used; transaction validation was disabled. The [custody report](CUSTODY_PROBE.md) records exact deposit/withdrawal balances, allowance consumption, rollback, foreign/deployer rejection, uncredited donation surplus and maximum allowance reduction. The independent offline JavaScript assertions check exact requests, values, successful events, revert bytes and persistent pinned-state reads. PublicNode's authenticated-archive requirement prevented a second complete run; its failure is preserved. One malformed disposable-address preflight was rejected before simulation and corrected with a local format guard; its restricted receipt remains retained.

These are executable source/compilation and provider-reported simulation observations, not independent local EVM execution or a deployed protocol. The source proof does not establish consensus; the remote sequence does not authenticate wallets or NFT ownership, establish finality/freshness, or create persistent custody. The probe's reentrancy/false-return/delta defenses were reviewed but were not exercised against adversarial token implementations in this sequence. The next independent local experiment must include those cases. Same-team review is not an external audit or community agreement.

All59 requirements and15 open conflicts retain their pinned text. The zero-address burn contradiction remains. Application behavior is unchanged apart from release metadata; test registration and research artifacts changed. The capture scripts, compiler inputs and Solidity experiments are outside the app's runtime and build output. Browser interactions were not rerun. No wallet, transaction broadcast, public website or chain deployment, storage offload or deletion occurred.
