# Validation boundary

Alpha.16 passed **326/326 tests across 21 files** on Node 24.20.0, with no failed, cancelled, skipped or pending tests. The [60-file source manifest](../evidence/CODE_SHA256SUMS.txt) identifies reviewed sources and captured fixtures. The supervised static build wrote and rehashed 22 files. See [current results](../evidence/TEST_RESULTS.json); the [alpha.15 receipt](../evidence/ALPHA15_TEST_RESULTS.json) remains available for the historical critique review.

Six new offline tests validate two preserved provider captures containing 36 read-only RPC calls at one pinned block. They assert every exact request, compare runtime and metadata, decode successful controls and zero-address rejection reasons, verify the dRPC account witness and match its code hash to both runtime copies. Changed claims, wrong roots and missing nodes reject. PublicNode's proof-window failure and the ambiguous burn-selector rejection remain preserved failures.

This is actual captured account-proof interoperability for this address/root, extending the earlier synthetic fixtures. No token storage slots were proved. The account proof does not execute the token or establish the correctness of provider call results; it commits to the runtime under a supplied root. Provider agreement does not authenticate consensus, bind a verified block header, establish freshness, prove NFT ownership or restore live permission. Exact token compiler/source correspondence and positive custody/supply changes remain unverified. No isolated fork, smart-contract implementation or chain deployment is claimed.

The [token compatibility report](CAW_TOKEN_COMPATIBILITY.md) records the literal zero-address incompatibility observed in the bounded calls. C-001 stays open. The original 59 requirements and 15 conflicts retain their pinned historical text; an observed failure does not amend the manifesto.

Existing application interaction/accounting logic is unchanged. Release metadata and test registration changed. The optional capture script is not connected to the app, build or test runner. Browser interactions were not rerun; no browser or hosting result is claimed. Same-team review is not an external audit or community agreement. No wallet, transaction, deployment, upload, research offload or deletion occurred in this release. The user-approved code-only MIT grant excludes the artwork, primary sources and raw network captures as recorded in [licensing status](../LICENSE_STATUS.md).
