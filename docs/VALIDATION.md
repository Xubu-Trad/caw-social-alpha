# Validation boundary

Alpha.18 passed **343/343 tests across 23 files**, with no failed, cancelled, skipped or pending tests. The [83-input manifest](../evidence/CODE_SHA256SUMS.txt), [current receipt](../evidence/TEST_RESULTS.json) and [alpha.17 receipt](../evidence/ALPHA17_TEST_RESULTS.json) keep releases distinct. The bounded static build rehashed 22 assets. Application behavior is unchanged apart from release metadata; no new browser result is claimed.

The original custody probe ran locally under checksum-pinned Anvil 1.8.1, chain 31337 and London EVM rules. The historical fork completed 23 cases through 24 local transactions; 519 local RPC requests and 29 forwarded historical reads were retained. The separate empty-chain synthetic suite completed 26 cases through 143 setup/action/recovery transactions and 1,482 local RPC requests, with no upstream reads. Both nodes exited and neither test port remained listening. These are local test transactions, not Ethereum broadcasts or ownership-authenticated actions.

Independent offline assertions cross-check exact source/build/runtime inputs, calls, return/error bytes, events, balances, allowance, supply, credits, callbacks and recovery against separately authored expectations. Malformed return data and wrong deltas reverted. Caught callbacks left one intended outer operation; propagated reentrancy failures rolled back. Trailing data after a valid true word was accepted. A synthetic extra sender fee was also accepted on deposit, exposing the missing sender-debit guarantee. Backing loss blocked credited operations until the measured uncredited replenishment. See [local custody](LOCAL_CUSTODY.md).

The actual token code/storage was not replaced in the fork. Synthetic substitution occurred only on a separate empty chain and does not describe real CAW behavior. Public test addresses were impersonated and local native gas balances were fabricated. The source/header/root remain external trust inputs. Local execution does not authenticate NFT ownership, Ethereum consensus, finality, freshness, historical full-block replay or persistent public custody. The prior metadata difference remains; original constructor execution is still not established.

Anvil has a Windows Job Object 512 MiB committed-memory cap, one-process limit and kill-on-close. Additional memory sampling, ten-minute node deadline, output/request/gas caps and startup floors are separate controls. Sampled node peaks were below 26 MiB; they are not exact OS peaks or supported-device minimums. The fixture compiled locally under the existing hash-pinned Solidity 0.8.10 compiler with no error diagnostics. No production toolchain choice is made.

All 59 requirements and 15 source conflicts retain their original text/status. The existing token still rejects literal zero-address transfers; username registration is gated. NFT authority, production paid settlement, durable public-history recovery and other protocol requirements remain unfinished. The test runner and native EVM are outside the frontend and static build. No real wallet, public deployment, storage offload or deletion occurred. Same-team review is not an external audit or community approval.
